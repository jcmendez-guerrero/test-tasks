---
artifacts:
  dockers:
    redis: "redis:5.0.3-alpine3.9"
    mariadb: "juanchocol69/mariadb:latest"
    rabbitmq: "juanchocol69/rabbitmq"

  rpm:
    setuptools:
      name: python3-setuptools
      uninstall: false
      deps: true
    pip:
      name: python3-pip
      uninstall: false
      deps: true

  pip:
    docker:
      name: "docker"
      version: "5.0.0"
      deps: true


